Quick commands (Visual Studio command prompt):

mkdir build
cd build
cmake -G "Visual Studio 17 2022" ..
cmake --build . --config Release

Executable will be in build/bin/Release or the Visual Studio output directory.
